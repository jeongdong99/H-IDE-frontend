//여기는 로그인된 사람이 로그인이나 회원가입페이지를 들어가지 못하게 하는 곳
//만약 들어간다면, register 페이지로 redirect할 수 있게

import React from 'react'

const NotAuthRoutes = () => {
  return (
    <div>NotAuthRoutes</div>
  )
}

export default NotAuthRoutes